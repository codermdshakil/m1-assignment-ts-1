


interface User{
    // properties
    name:string;
    age:number;
};

const user1 : User = {
    name:"Shakil Ahmed",
    age:21
}

type Status = "success" | "error" | "loading";

type ID = string | number;